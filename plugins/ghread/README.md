# GitHub Read-Only (gh)

A tool window that lets you browse GitHub repositories, issues, pull requests and more using the `gh` CLI. The plugin is read-only and never modifies data on GitHub.

## Enabling

The feature is disabled by default. Enable **GitHub Read** in *Settings | Tools | GitHub Read* to show the tool window.
